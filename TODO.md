also show price chart of both prediction market and token in ui on token page

ensure all dollar values in the UI are prefixed with $ instead of just being raw numbers.

ensure liquidity value + pm trading volume is displayed on ui per mkt.

for each token show its underlying mktcap

# ignore for now
- deferred: calibrate period and drawdown % from historical data
- create trading bot
- make res based on a specific price threshold. if if becomes stale due to appreciation, then create new mkt and deprecate old one
- improve ui a lot. it sucks rn